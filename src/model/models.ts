export interface ISetting {
  volumeRange: string,
  volumeOff: boolean,
  gameTimerOff: boolean,
  timeAnswer: number
}

export interface Igenre {
  activeGenre: string,
}

export interface Itimer {
  timerActive: boolean
}